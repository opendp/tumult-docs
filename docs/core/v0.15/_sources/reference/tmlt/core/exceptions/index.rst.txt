


*****************
exceptions
*****************

.. py:module:: tmlt.core.exceptions

.. autoapi-nested-parse::

   Special exceptions raised by Core in certain situations.












Exceptions
==========

.. list-table::

   * - :exc:`.OutOfDomainError`
     - Exception type that indicates a validation error in a Domain.
   * - :exc:`.DomainMismatchError`
     - Exception type raised when two or more domains should match, but don't.
   * - :exc:`.UnsupportedDomainError`
     - Exception type that indicates that a given domain is not supported.
   * - :exc:`.DomainKeyError`
     - Exception type that indicates that a key is not in the given domain.
   * - :exc:`.DomainColumnError`
     - Exception type for when a column is not in the given domain's schema.
   * - :exc:`.UnsupportedMetricError`
     - Exception raised when a given metric is not supported.
   * - :exc:`.MetricMismatchError`
     - Exception raised when two or more metrics should match, but don't.
   * - :exc:`.UnsupportedCombinationError`
     - Raised when a given combination of values is not supported.
   * - :exc:`.UnsupportedMeasureError`
     - Error raised when a given measure is not supported.
   * - :exc:`.MeasureMismatchError`
     - Error raised when two or more measures should match, but don't.
   * - :exc:`.UnsupportedNoiseMechanismError`
     - Error raised when the given noise mechanism is not supported.
   * - :exc:`.UnsupportedSympyExprError`
     - Error raised when trying to use an unsupported Sympy expression.

   
.. py:exception:: OutOfDomainError(domain, value, msg)

   Bases: :class:`Exception`

   Exception type that indicates a validation error in a Domain.

   .. attribute:: domain

      The `~tmlt.core.domains.base.Domain` on which the exception
      was raised.

   .. attribute:: value

      The value that is not in the domain.




   
.. py:exception:: DomainMismatchError(domains, msg)

   Bases: :class:`ValueError`

   Exception type raised when two or more domains should match, but don't.

   .. attribute:: domains

      An `Iterable` of all the  `~tmlt.core.domains.base.Domain`
      that should match, but do not.




   
.. py:exception:: UnsupportedDomainError(domain, msg)

   Bases: :class:`TypeError`

   Exception type that indicates that a given domain is not supported.

   .. attribute:: domain

      The `~tmlt.core.domains.base.Domain` that is not supported.




   
.. py:exception:: DomainKeyError(domain, key, msg)

   Bases: :class:`Exception`

   Exception type that indicates that a key is not in the given domain.

   .. attribute:: domain

      The `~tmlt.core.domains.base.Domain` on which this error was raised.

   .. attribute:: key

      The key that was not in the domain, or a collection of keys that are
      not in the domain.




   
.. py:exception:: DomainColumnError(domain, column, msg)

   Bases: :class:`Exception`

   Exception type for when a column is not in the given domain's schema.




   
.. py:exception:: UnsupportedMetricError(metric, msg)

   Bases: :class:`ValueError`

   Exception raised when a given metric is not supported.

   .. attribute:: metric

      The metric that is not supported.




   
.. py:exception:: MetricMismatchError(metrics, msg)

   Bases: :class:`ValueError`

   Exception raised when two or more metrics should match, but don't.

   .. attribute:: metrics

      The metrics that should match, but do not.




   
.. py:exception:: UnsupportedCombinationError(combination, msg)

   Bases: :class:`ValueError`

   Raised when a given combination of values is not supported.

   If this exception is raised, any of the values used may be *individually*
   valid, but they are not valid *in this particular combination*.

   For example, when a metric does not support a given domain, this is the
   error that is raised.

   .. attribute:: combination

      The combination of values that are not supported.




   
.. py:exception:: UnsupportedMeasureError(measure, msg)

   Bases: :class:`ValueError`

   Error raised when a given measure is not supported.

   .. attribute:: measure

      The `~tmlt.core.measures.Measure` that is not supported.




   
.. py:exception:: MeasureMismatchError(measures, msg)

   Bases: :class:`ValueError`

   Error raised when two or more measures should match, but don't.

   .. attribute:: measures

      An `~Iterable` with multiple `~tmlt.core.measures.Measure`
      that should match, but don't.




   
.. py:exception:: UnsupportedNoiseMechanismError(noise_mechanism, msg)

   Bases: :class:`ValueError`

   Error raised when the given noise mechanism is not supported.

   .. attribute:: noise_mechanism

      The `~tmlt.core.measurements.aggregations.NoiseMechanism`
      that is not supported.




   
.. py:exception:: UnsupportedSympyExprError(expr, msg)

   Bases: :class:`ValueError`

   Error raised when trying to use an unsupported Sympy expression.

   This error is raised when an
   `~tmlt.core.utils.exact_number.ExactNumber` cannot be created from a given
   SymPy expression. This might be raised because the expression corresponds
   to an imaginary number, because the expression has a free variable, or
   because the expression is a kind of Sympy expression that ExactNumber
   does not support (such as sums).

   .. attribute:: expr

      the invalid Sympy expression.






