
..
    SPDX-License-Identifier: CC-BY-SA-4.0
    Copyright Tumult Labs 2024


**************
program
**************

.. py:module:: tmlt.analytics.program

.. autoapi-nested-parse::

   SessionProgram and SessionProgramBuilder interfaces.

   SessionPrograms are used to define structured DP programs that rely on the
   privacy protection provided by the :class:`~.session.Session` API. By defining a
   standard interface for creating and running these programs, we can build higher level
   tools that can interact with them in a consistent way, such as
   :class:`~.tuner.SessionProgramTuner`.

   The :class:`~.SessionProgram` class is an abstract base class that defines the interface
   for a structured DP program. It is designed to be subclassed to define specific
   programs.

   Every :class:`~.SessionProgram` has three minimal requirements:

       * Defines at least one protected input, which is a DataFrame that can only be
         accessed through the :class:`~.session.Session` API.
       * Defines at least one output, which is a DataFrame that is produced by the program.
       * Defines a :meth:`~.SessionProgram.session_interaction` method that takes a session
         as an argument and returns a dictionary containing the expected outputs.

   .. ::

       >>> from tmlt.analytics.program import SessionProgram
       >>> from tmlt.analytics.privacy_budget import PureDPBudget
       >>> from tmlt.analytics.protected_change import AddOneRow
       >>> from tmlt.analytics.session import Session
       >>> from tmlt.analytics.query_builder import QueryBuilder
       >>> from pyspark.sql import DataFrame
       >>> from pyspark.sql import SparkSession
       >>> spark = SparkSession.builder.getOrCreate()

   >>> class MinimalProgram(SessionProgram):
   ...     class ProtectedInputs:
   ...         protected_df: DataFrame  # DataFrame type annotation is required
   ...     class Outputs:
   ...         total_count: DataFrame  # required here too
   ...     def session_interaction(self, session: Session):
   ...         count_query = QueryBuilder("protected_df").count()
   ...         budget = self.privacy_budget  #  session.remaining_privacy_budget also works
   ...         total_count = session.evaluate(count_query, budget)
   ...         return {"total_count": total_count}

   Once a program is defined, it can be instantiated by using the automatically
   generated builder for that class. It has a very similar interface to
   :class:`~.session.Session.Builder`.

   >>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
   >>> program = (
   ...     MinimalProgram.Builder()
   ...     .with_privacy_budget(PureDPBudget(epsilon=1))
   ...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
   ...     .build()
   ... )

   The program can then be run to produce the expected outputs.

   >>> program.run()
   >>> program.outputs
   {'total_count': DataFrame[count: bigint]}

   Each instance of a program has the same privacy guarantee as a
   :class:`~.session.Session` with the same privacy budget, protected dataframes, and
   protected changes.

   Because of this, each instance of a program can only be run once. To run the program
   again, you must create a new instance of the program.

   >>> program.run()
   Traceback (most recent call last):
   ...
   RuntimeError: run cannot be called more than once

   Most of the time, you will also want to define :class:`~.Parameters` and/or
   :class:`~.UnprotectedInputs` for your program. This can be done by adding them to the
   program class similar to :class:`~.ProtectedInputs` and :class:`~.Outputs`.

   Additionally, once you have structured your program into a :class:`~.SessionProgram`,
   you will want to take advantage of tools like :class:`~.tuner.SessionProgramTuner`.

   See the tutorials starting at <# TODO: add link> for more examples on how to take
   advantage of :class:`~.SessionProgram` and related classes.











Classes
=======

.. list-table::

   * - :class:`.SessionProgramBuilder`
     - Abstract builder for a SessionProgram.
   * - :class:`.SessionProgram`
     - Base class for defining a structured DP program that uses the Session API.

   
..
    SPDX-License-Identifier: CC-BY-SA-4.0
    Copyright Tumult Labs 2024
.. py:class:: SessionProgramBuilder

   Bases: :class:`tmlt.analytics._base_builder.BaseBuilder`, :class:`tmlt.analytics._base_builder.PrivacyBudgetMixin`, :class:`tmlt.analytics._base_builder.DataFrameMixin`, :class:`tmlt.analytics._base_builder.ParameterMixin`, :class:`abc.ABC`

   Abstract builder for a SessionProgram.






   .. automethod:: __init__
   .. py:method:: build()
      :abstractmethod:

      Construct the type that this builder builds.


   .. py:method:: with_privacy_budget(privacy_budget)

      Set the privacy budget for the object being built.


   .. py:method:: with_private_dataframe(source_id, dataframe, protected_change)

      Adds a Spark DataFrame as a private source.

      Not all Spark column types are supported in private sources; see
      :data:`tmlt.analytics.session.SUPPORTED_SPARK_TYPES` for information about
      which types are supported.

      :param source_id: Source id for the private source dataframe.
      :param dataframe: Private source dataframe to perform queries on,
                        corresponding to the ``source_id``.
      :param protected_change: A
                               :class:`~tmlt.analytics.protected_change.ProtectedChange`
                               specifying what changes to the input data should be protected.


   .. py:method:: with_public_dataframe(source_id, dataframe)

      Add a public dataframe.


   .. py:method:: with_id_space(id_space)

      Add an identifier space.

      This defines a space of identifiers that map 1-to-1 to the identifiers
      being protected by a table with the :class`~AddRowsWithID` protected
      change. Any table with such a protected change must be a member of some
      identifier space.


   .. py:method:: with_parameter(name, value)

      Set the value of a parameter.



   
..
    SPDX-License-Identifier: CC-BY-SA-4.0
    Copyright Tumult Labs 2024
.. py:class:: SessionProgram(builder)

   Bases: :class:`abc.ABC`

   Base class for defining a structured DP program that uses the Session API.

   .. note::

       This class is only available on a paid version of Tumult Analytics. If you
       would like to hear more, please contact us at info@tmlt.io.

   SessionPrograms should not be directly constructed. Instead, they should be created
   using the :class:`~.SessionProgramBuilder` that is automatically generated for each
   subclass of :class:`~.SessionProgram` which can be accessed using the `Builder`
   attribute of the subclass.



   .. list-table:: Classes

      * - :class:`.ProtectedInputs`
        - An annotation class for protected inputs to a SessionProgram.
      * - :class:`.UnprotectedInputs`
        - An annotation class for unprotected inputs to a SessionProgram.
      * - :class:`.Parameters`
        - An annotation class for parameters to a SessionProgram.
      * - :class:`.Outputs`
        - An annotation class for the outputs of a SessionProgram.

   .. list-table:: Attributes

      * - :attr:`.Builder`
        - The builder class for this SessionProgram.

   .. list-table:: Methods

      * - :meth:`.privacy_budget`
        - The privacy budget for this program.
      * - :meth:`.unprotected_inputs`
        - The unprotected inputs for this program.
      * - :meth:`.parameters`
        - The parameters for this program.
      * - :meth:`.run`
        - Run the program.
      * - :meth:`.outputs`
        - The outputs after running this program.
      * - :meth:`.session_interaction`
        - The interaction with the session that this program performs.


   
   ..
       SPDX-License-Identifier: CC-BY-SA-4.0
       Copyright Tumult Labs 2024
   .. py:class:: ProtectedInputs

      An annotation class for protected inputs to a SessionProgram.

      ProtectedInput classes enumerate the expected protected dataframes that will
      be used in the program. These are the dataframes that will be protected by
      differential privacy according to their protected change and the privacy
      budget provided in the builder.

      Each protected dataframe can be specified in the builder using
      :meth:`~.SessionProgramBuilder.with_private_dataframe`, and they will be
      accessible in the :meth:`~.SessionProgram.session_interaction` method as a
      private source with the same name in the given session.

      .. rubric:: Example

      ..
          >>> from tmlt.analytics.program import SessionProgram
          >>> from tmlt.analytics.privacy_budget import PureDPBudget
          >>> from tmlt.analytics.protected_change import AddOneRow
          >>> from tmlt.analytics.session import Session
          >>> from tmlt.analytics.query_builder import QueryBuilder
          >>> from pyspark.sql import DataFrame
          >>> from pyspark.sql import SparkSession
          >>> spark = SparkSession.builder.getOrCreate()

      >>> class ProgramWithProtectedInputs(SessionProgram):
      ...     class ProtectedInputs:
      ...         protected_df: DataFrame
      ...     class Outputs:
      ...         total_count: DataFrame
      ...     def session_interaction(self, session: Session):
      ...         print("Private sources:", session.private_sources)
      ...         count_query = QueryBuilder("protected_df").count()
      ...         total_count = session.evaluate(count_query, self.privacy_budget)
      ...         return {"total_count": total_count}
      >>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
      >>> program = (
      ...     ProgramWithProtectedInputs.Builder()
      ...     .with_privacy_budget(PureDPBudget(epsilon=1))
      ...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
      ...     .build()
      ... )
      >>> program.run()
      Private sources: ['protected_df']
      >>> program.outputs
      {'total_count': DataFrame[count: bigint]}







   
   ..
       SPDX-License-Identifier: CC-BY-SA-4.0
       Copyright Tumult Labs 2024
   .. py:class:: UnprotectedInputs

      An annotation class for unprotected inputs to a SessionProgram.

      UnprotectedInputs classes enumerate the expected unprotected dataframes that
      will be used by the program. These dataframes are not protected by differential
      privacy, and can be accessed directly by the program. They are most often used
      to provide large amounts of public information such as required for a join, or
      to specify a :class:`~.KeySet`.

      Each unprotected dataframe can be specified in the builder using
      :meth:`~.SessionProgramBuilder.with_public_dataframe`, and they will be
      accessible in the :meth:`~.SessionProgram.session_interaction` method as a
      public source with the same name in the given session, or through the
      :attr:`~.SessionProgram.unprotected_inputs` property.

      .. rubric:: Example

      ..
          >>> from tmlt.analytics.program import SessionProgram
          >>> from tmlt.analytics.privacy_budget import PureDPBudget
          >>> from tmlt.analytics.protected_change import AddOneRow
          >>> from tmlt.analytics.session import Session
          >>> from tmlt.analytics.query_builder import QueryBuilder
          >>> from pyspark.sql import DataFrame
          >>> from pyspark.sql import SparkSession
          >>> spark = SparkSession.builder.getOrCreate()

      >>> class ProgramWithUnprotectedInputs(SessionProgram):
      ...     class ProtectedInputs:
      ...         protected_df: DataFrame
      ...     class UnprotectedInputs:
      ...         public_df: DataFrame
      ...     class Outputs:
      ...         total_count: DataFrame
      ...     def session_interaction(self, session: Session):
      ...         print("Public sources:", session.public_sources)
      ...         assert session.public_source_dataframes == {
      ...             "public_df": public_df
      ...         }
      ...         assert self.unprotected_inputs == {"public_df": public_df}
      ...         count_query = QueryBuilder("protected_df").count()
      ...         total_count = session.evaluate(count_query, self.privacy_budget)
      ...         return {"total_count": total_count}
      >>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
      >>> public_df = spark.createDataFrame([(1, 2), (3, 4)], ["c", "d"])
      >>> program = (
      ...     ProgramWithUnprotectedInputs.Builder()
      ...     .with_privacy_budget(PureDPBudget(epsilon=1))
      ...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
      ...     .with_public_dataframe("public_df", public_df)
      ...     .build()
      ... )
      >>> program.run()
      Public sources: ['public_df']
      >>> program.outputs
      {'total_count': DataFrame[count: bigint]}







   
   ..
       SPDX-License-Identifier: CC-BY-SA-4.0
       Copyright Tumult Labs 2024
   .. py:class:: Parameters

      An annotation class for parameters to a SessionProgram.

      Parameters classes enumerate the expected parameters that will be used by the
      program. These parameters are not dataframes, but are instead values that are
      used to control the behavior of the program. They are most often used to
      configure the behavior of the program, such as setting thresholds, clamping
      bounds, budget allocations, choosing among algorithms, etc.

      Each parameter can be specified in the builder using
      :meth:`~.SessionProgramBuilder.with_parameter`, and they will be accessible in
      the :meth:`~.SessionProgram.session_interaction` by using the
      :attr:`~.SessionProgram.parameters` property.

      .. rubric:: Example

      ..
          >>> from tmlt.analytics.program import SessionProgram
          >>> from tmlt.analytics.privacy_budget import PureDPBudget
          >>> from tmlt.analytics.protected_change import AddOneRow
          >>> from tmlt.analytics.session import Session
          >>> from tmlt.analytics.query_builder import QueryBuilder
          >>> from pyspark.sql import DataFrame
          >>> from pyspark.sql import SparkSession
          >>> spark = SparkSession.builder.getOrCreate()

      >>> class ProgramWithParameters(SessionProgram):
      ...     class ProtectedInputs:
      ...         protected_df: DataFrame
      ...     class Outputs:
      ...         a_sum: DataFrame
      ...     class Parameters:
      ...         low: int
      ...         high: int
      ...     def session_interaction(self, session: Session):
      ...         low = self.parameters["low"]
      ...         high = self.parameters["high"]
      ...         sum_query = QueryBuilder("protected_df").sum("a", low, high)
      ...         a_sum = session.evaluate(sum_query, self.privacy_budget)
      ...         return {"a_sum": a_sum}
      >>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
      >>> program = (
      ...     ProgramWithParameters.Builder()
      ...     .with_privacy_budget(PureDPBudget(epsilon=1))
      ...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
      ...     .with_parameter("low", 0)
      ...     .with_parameter("high", 5)
      ...     .build()
      ... )
      >>> program.run()
      >>> program.outputs
      {'a_sum': DataFrame[a_sum: bigint]}







   
   ..
       SPDX-License-Identifier: CC-BY-SA-4.0
       Copyright Tumult Labs 2024
   .. py:class:: Outputs

      An annotation class for the outputs of a SessionProgram.

      These outputs are expected to be returned by the
      :meth:`~.SessionProgram.session_interaction` method as a dictionary, where the
      keys are the names of the outputs and the values are the corresponding
      DataFrames.

      .. rubric:: Example

      ..
          >>> from tmlt.analytics.program import SessionProgram
          >>> from tmlt.analytics.privacy_budget import PureDPBudget
          >>> from tmlt.analytics.protected_change import AddOneRow
          >>> from tmlt.analytics.session import Session
          >>> from tmlt.analytics.query_builder import QueryBuilder
          >>> from pyspark.sql import DataFrame
          >>> from pyspark.sql import SparkSession
          >>> spark = SparkSession.builder.getOrCreate()

      >>> class ProgramWithOutputs(SessionProgram):
      ...     class ProtectedInputs:
      ...         protected_df: DataFrame
      ...     class Outputs:
      ...         total_count: DataFrame
      ...     def session_interaction(self, session: Session):
      ...         count_query = QueryBuilder("protected_df").count()
      ...         total_count = session.evaluate(count_query, self.privacy_budget)
      ...         return {"total_count": total_count}
      >>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
      >>> program = (
      ...     ProgramWithOutputs.Builder()
      ...     .with_privacy_budget(PureDPBudget(epsilon=1))
      ...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
      ...     .build()
      ... )
      >>> program.run()
      >>> program.outputs
      {'total_count': DataFrame[count: bigint]}








   .. py:attribute:: Builder
      :annotation: :Type[SessionProgramBuilder]

      The builder class for this SessionProgram.


   .. automethod:: __init__
   .. py:method:: privacy_budget()
      :property:

      The privacy budget for this program.


   .. py:method:: unprotected_inputs()
      :property:

      The unprotected inputs for this program.


   .. py:method:: parameters()
      :property:

      The parameters for this program.


   .. py:method:: run()

      Run the program.

      The results of the program can be accessed using the `outputs` property after
      calling this method.

      Note that this method can only be called once. If you need to run the program
      again, you must create a new instance of the program.


   .. py:method:: outputs()
      :property:

      The outputs after running this program.


   .. py:method:: session_interaction(session)
      :abstractmethod:

      The interaction with the session that this program performs.

      This method should be overridden by subclasses to generate the expected outputs
      of the program using the given session. The method should return a dictionary
      of the expected outputs, where the keys are the names of the outputs and the
      values are the corresponding DataFrames.

      .. warning::

          Do not call this method directly. Instead, call the :meth:`run` method.

      :param session: The session to interact with. It will be initialized with the
                      protected and unprotected dataframes as well as the privacy budget.







