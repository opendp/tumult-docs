.. _programs:

Defining and using programs
===========================

..
    Copyright Tumult Labs 2025. All rights reserved.
    This file is part of Tumult Analytics Pro.
    It may only be used, reproduced, modified or distributed pursuant to
    Tumult Labs' Enterprise License Agreement or another commercial license
    agreement entered into between you and Tumult Labs.
    No other rights are granted to use this file in any way.

.. note::

    |PRO_NOTE|

:class:`~tmlt.analytics.SessionProgram`\ s are used to define structured DP
programs that rely on the privacy protection provided by the
:class:`~tmlt.analytics.Session` API. By defining a standard interface for
creating and running these programs, we can build higher level tools that can
interact with them in a consistent way, such as
:class:`~tmlt.tune.SessionProgramTuner`.

The :class:`~tmlt.analytics.SessionProgram` class is an abstract base class that defines
the interface for a structured DP program. It is designed to be subclassed to define
specific programs.

Every :class:`~tmlt.analytics.SessionProgram` has three minimal requirements:

* Defines at least one protected input, which is a DataFrame that can only be
  accessed through the :class:`~.Session` API.
* Defines at least one output, which is a DataFrame that is produced by the program.
* Defines a :meth:`~tmlt.analytics.SessionProgram.session_interaction` method that takes
  a :class:`~tmlt.analytics.Session` as an argument and returns a dictionary containing
  the expected outputs.

.. ::

    >>> from tmlt.analytics import (
    ...     AddOneRow,
    ...     PureDPBudget,
    ...     QueryBuilder,
    ...     Session,
    ...     SessionProgram,
    ... )
    >>> from pyspark.sql import DataFrame
    >>> from pyspark.sql import SparkSession
    >>> spark = SparkSession.builder.getOrCreate()

>>> class MinimalProgram(SessionProgram):
...     class ProtectedInputs:
...         protected_df: DataFrame  # DataFrame type annotation is required
...     class Outputs:
...         total_count: DataFrame  # required here too
...     def session_interaction(self, session: Session):
...         count_query = QueryBuilder("protected_df").count()
...         budget = self.privacy_budget  #  session.remaining_privacy_budget also works
...         total_count = session.evaluate(count_query, budget)
...         return {"total_count": total_count}

Once a program is defined, it can be instantiated by using the automatically
generated :class:`~tmlt.analytics.SessionProgram.Builder` for that class. It has a very
similar interface to :class:`Session.Builder<tmlt.analytics.Session.Builder>`.

>>> protected_df = spark.createDataFrame([(1, 2), (3, 4)], ["a", "b"])
>>> program = (
...     MinimalProgram.Builder()
...     .with_privacy_budget(PureDPBudget(epsilon=1))
...     .with_private_dataframe("protected_df", protected_df, AddOneRow())
...     .build()
... )

The program can then be run to produce the expected outputs.

>>> program.run()
{'total_count': DataFrame[count: bigint]}

Each instance of a program has the same privacy guarantee as a :class:`~.Session` with
the same privacy budget, protected DataFrames, and protected changes. Therefore, each
instance of a program can only be run once. To run the program again, users must create
a new instance of the program (which will consume additional privacy loss budget).

>>> program.run()
Traceback (most recent call last):
...
RuntimeError: run cannot be called more than once

Often, users will also want to define :class:`~tmlt.analytics.SessionProgram.Parameters`
and/or :class:`~tmlt.analytics.SessionProgram.UnprotectedInputs` in their programs. This
can be done by adding them to the program class similar to
:class:`~tmlt.analytics.SessionProgram.ProtectedInputs` and
:class:`~tmlt.analytics.SessionProgram.Outputs`.

Once a program is structured into a :class:`~.SessionProgram`, users can take advantage
of tools like :class:`~tmlt.tune.SessionProgramTuner`. More information can be found in
the tutorials starting at :ref:`Basics of error measurement`.

.. currentmodule:: tmlt.analytics

Defining and initializing a program
-----------------------------------
.. autosummary::
   :toctree: api/
   :nosignatures:

   SessionProgram
   SessionProgram.Builder

Inspecting a program
--------------------
.. autosummary::
   :toctree: api/

   SessionProgram.privacy_budget
   SessionProgram.unprotected_inputs
   SessionProgram.parameters
   SessionProgram.output_types

Running a program
-----------------
.. autosummary::
   :toctree: api/

   SessionProgram.run
