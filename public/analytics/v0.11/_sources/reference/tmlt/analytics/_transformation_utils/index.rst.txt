:orphan:

..
    SPDX-License-Identifier: CC-BY-SA-4.0
    Copyright Tumult Labs 2024


****************************
_transformation_utils
****************************

.. py:module:: tmlt.analytics._transformation_utils

.. autoapi-nested-parse::

   Useful functions to be used with transformations.











Functions
=========

.. list-table::

   * - :func:`.generate_nested_transformation`
     - Generate a nested transformation.
   * - :func:`.rename_table`
     - Renames tables.
   * - :func:`.delete_table`
     - Deletes tables.
   * - :func:`.persist_table`
     - Persists tables.
   * - :func:`.unpersist_table`
     - Unpersists tables.
   * - :func:`.get_table_from_ref`
     - Returns a GetValue transformation finding the table specified.

.. py:function:: generate_nested_transformation(base_transformation, parent_reference, generator_dict, table_identifier = None)

   Generate a nested transformation.

   At a high level, this function chooses an appropriate transformation
   by keying into the generator dictionary at the parent_reference's Metric,
   then applies the selected transformation at parent_reference in the output
   of base_transformation.

   :param base_transformation: Transformation to be used as a starting point.
   :param parent_reference: The parent TableReference of base_transformation's associated
                            TableReference.
   :param generator_dict: a dictionary of the form { Metric : generator() },
                          where generator() is a function which takes the associated domain and metric
                          of the parent_reference and generates a transformation using the
                          Metric, e.g. generator(parent_domain, parent_metric, target_identifier)
   :param table_identifier: identifier for new table in case of rename and original
                            table in case of delete/persist/unpersist



.. py:function:: rename_table(base_transformation, base_ref, new_table_id)

   Renames tables.

   A single value is transformed and added to the dictionary at a new key.
   Note that the original value is left unchanged in the dictionary.



.. py:function:: delete_table(base_transformation, base_ref)

   Deletes tables.



.. py:function:: persist_table(base_transformation, base_ref, new_table_id = None)

   Persists tables.



.. py:function:: unpersist_table(base_transformation, base_ref)

   Unpersists tables.



.. py:function:: get_table_from_ref(transformation, ref)

   Returns a GetValue transformation finding the table specified.








