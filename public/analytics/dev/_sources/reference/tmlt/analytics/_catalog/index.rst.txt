:orphan:



***************
_catalog
***************

.. py:module:: tmlt.analytics._catalog

.. autoapi-nested-parse::

   Contains classes for specifying schemas and constraints for tables.











Classes
=======

.. list-table::

   * - :class:`.Table`
     - Metadata for a public or private table.
   * - :class:`.PublicTable`
     - Metadata for a public table.
   * - :class:`.PrivateTable`
     - Metadata for a private table.
   * - :class:`.PrivateView`
     - Metadata for a view on a private table.
   * - :class:`.Catalog`
     - Specifies schemas and constraints on public and private tables.

   
.. py:class:: Table

   Bases: :class:`abc.ABC`

   Metadata for a public or private table.





   .. py:attribute:: source_id
      :annotation: :str

      The source id, or unique identifier, for the table.

   .. py:attribute:: schema
      :annotation: :tmlt.analytics._schema.Schema

      The analytics schema for the table. Describes the column types.



   
.. py:class:: PublicTable

   Bases: :class:`Table`

   Metadata for a public table.

   Public tables contain information that is generally not sensitive and does not
   require any special privacy protections.





   .. py:attribute:: source_id
      :annotation: :str

      The source id, or unique identifier, for the table.

   .. py:attribute:: schema
      :annotation: :tmlt.analytics._schema.Schema

      The analytics schema for the table. Describes the column types.



   
.. py:class:: PrivateTable

   Bases: :class:`Table`

   Metadata for a private table.

   Private tables contain sensitive information, such as PII, whose privacy has to be
   protected.





   .. py:attribute:: stability
      :annotation: :int

      The maximum number of rows a single individual can modify if they are modified.

      This can become higher than 1 if there have been transformations, such as joins,
      which allow single individuals to affect many rows in the table.

   .. py:attribute:: source_id
      :annotation: :str

      The source id, or unique identifier, for the table.

   .. py:attribute:: schema
      :annotation: :tmlt.analytics._schema.Schema

      The analytics schema for the table. Describes the column types.



   
.. py:class:: PrivateView

   Bases: :class:`Table`

   Metadata for a view on a private table.

   PrivateViews are similar to database views, which are extensions of the original
   tables. Information in a private view is private and needs to be protected.





   .. py:attribute:: stability
      :annotation: :int

      The maximum number of rows a single individual can modify if they are modified.

      This can become higher than 1 if there have been transformations, such as joins,
      which allow single individuals to affect many rows in the table.

   .. py:attribute:: source_id
      :annotation: :str

      The source id, or unique identifier, for the table.

   .. py:attribute:: schema
      :annotation: :tmlt.analytics._schema.Schema

      The analytics schema for the table. Describes the column types.



   
.. py:class:: Catalog

   Specifies schemas and constraints on public and private tables.






   .. automethod:: __init__
   .. py:method:: remove_table(self, source_id)

      Removes a table from the catalog.

      :param source_id: The name of the table.


   .. py:method:: add_private_source(self, source_id, col_types, stability, grouping_column = None)

      Adds a private table to catalog. There may only be a single private table.

      :param source_id: The source id, or unique identifier, for the private table.
      :param col_types: Mapping from column names to types for private table.
      :param stability: The maximum number of rows that could be added or removed in
                        the table if a single individual is added or removed.
      :param grouping_column: Name of the column (if any) that must be grouped by in any
                              groupby aggregations that use this table.

      :raises RuntimeError: If there is already a private table.


   .. py:method:: add_private_view(self, source_id, col_types, stability, grouping_column = None)

      Adds view table to catalog.

      :param source_id: The source id, or unique identifier, for the view table.
      :param col_types: Mapping from column names to types for private view.
      :param stability: The maximum number of rows that could be added or removed in
                        the table if a single individual is added or removed.
      :param grouping_column: Name of the column (if any) that must be grouped by in any
                              groupby aggregations that use this table.


   .. py:method:: add_public_source(self, source_id, col_types)

      Adds public table to catalog.

      :param source_id: The source id, or unique identifier, for the public table.
      :param col_types: Mapping from column names to types for the public table.


   .. py:method:: private_table(self)
      :property:

      Returns the primary private table, if it exists.


   .. py:method:: tables(self)
      :property:

      Returns the catalog as a dictionary of tables.







